import React, { FC, ReactElement } from 'react'
import "./pages-styles.css"

// state management
// import useStore from '../Store';

const Register: FC = (): ReactElement => {
    // const setUserToken = useStore(state => state.userToken)


    return (
        <div>Register</div>
    )
}

export default Register